// Assuming other necessary imports and setup code are present

const BASE_PRICE = 1799; // Updated price for flash sale

// Removal of referral code discount logic for SAPPHIRE250
// Existing conditional pricing logic (to be removed):
// if (isReferralCodeApplied && referralCode === 'SAPPHIRE250') {
//   price -= 250; // Discount applied
// }

function RegistrationPage() {
  const price = BASE_PRICE; // Direct assignment of base price
  // ... rest of the component logic
}

export default RegistrationPage;